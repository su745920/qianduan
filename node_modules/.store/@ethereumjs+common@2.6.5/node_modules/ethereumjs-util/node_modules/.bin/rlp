#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -z "$NODE_PATH" ]; then
  export NODE_PATH="$basedir/C:/Users/51009/Desktop/mini/去中心/bb_uniapp/node_modules/.store/rlp@2.2.7/node_modules"
else
  export NODE_PATH="$NODE_PATH:$basedir/C:/Users/51009/Desktop/mini/去中心/bb_uniapp/node_modules/.store/rlp@2.2.7/node_modules"
fi
if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../../rlp@2.2.7/node_modules/rlp/bin/rlp" "$@"
else
  exec node  "$basedir/../../../../../rlp@2.2.7/node_modules/rlp/bin/rlp" "$@"
fi
